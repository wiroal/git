# Comandos para sincronizar

git status
git show 
git add *
git commit -m "nombre-del-comit"
git push origin master
git pull

# LUEGO DE TRABAJAR EN EL LOCAL EJECUTAMOS
git add *
git commit -m "nombre-del-commit"
git push origin master
# PASO A PASO PARA CONFIGURAR LA LLAVE SSH 

    1) ssh-keygen -t rsa -b 4096 -C “tu_email@gmail.com” (Crea la llave, sigua el procedimiento)
    2) eval $(ssh-agent -s) (Revisa que esté cooriendo)
    3) ssh-add ~/.ssh/id_rsa (Agrega la llave privada a Git)
    4) Ssh agregar start-ssh-agent.cmd
    5) vi ~/.ssh/id_rsa.pub (copia la llave publica y la pega en github)